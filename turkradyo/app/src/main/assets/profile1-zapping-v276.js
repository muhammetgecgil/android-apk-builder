(()=>{'use strict';
const SEL='.modes > .mode[data-mode="favorites"]';
const ICON='<svg viewBox="0 0 24 24" aria-hidden="true" style="width:34px;height:34px;display:block;margin:auto;fill:currentColor;stroke:currentColor;stroke-width:1.2;stroke-linecap:round;stroke-linejoin:round"><path d="M13.3 2.5 5.9 13h5l-1 8.5 8.2-11.8h-5.2z"/></svg>';
function isP1(){try{return localStorage.getItem('p2Active')!=='1'&&!document.body.classList.contains('profile2-active')}catch(e){return!document.body.classList.contains('profile2-active')}}
function btn(){return document.querySelector(SEL)}
function patch(){const b=btn();if(!b)return;if(isP1()){delete b.dataset.p266Zap;b.dataset.p1Zap='1';b.innerHTML='<b>'+ICON+'</b>ZAPPING';b.setAttribute('aria-label','Zapping');b.title='Zapping • benzer ve sağlam radyoya geç'}else if(b.dataset.p1Zap==='1'){delete b.dataset.p1Zap;if(!b.dataset.p266Zap){b.innerHTML='<b>♥</b>FAVORİLER';b.removeAttribute('aria-label');b.removeAttribute('title')}}}
function k(s){return s?.stationuuid||s?._url||s?.url_resolved||s?.url||s?.name||''}
function genre(s){const t=((s?.name||'')+' '+(s?.tags||'')).toLocaleLowerCase('tr');if(/arabesk|fantezi|fantazi|damar/.test(t))return'Arabesk';if(/rock|metal|alternative|indie/.test(t))return'Rock';if(/türkü|turku|folk|halk/.test(t))return'Türkü';if(/klasik|classical|opera|senfoni/.test(t))return'Klasik';if(/haber|news|gündem|gundem/.test(t))return'Haber';if(/spor|sports/.test(t))return'Spor';if(/dini|ilah[iı]|tasavvuf/.test(t))return'Dini';if(/jazz|blues/.test(t))return'Jazz';if(/pop|hit|dance|top 40/.test(t))return'Pop';return'Diğer'}
function tags(s){return new Set(String(s?.tags||'').toLocaleLowerCase('tr').split(',').map(x=>x.trim()).filter(Boolean))}
function health(s){let x=50;x+=s?.lastcheckok?18:-12;x+=Math.min(12,(s?.bitrate||0)/32);x+=Math.min(10,(s?.clicktrend||0)/50);x+=Math.min(10,(s?.votes||0)/100);x+=Math.min(8,(s?.clickcount||0)/3000);return Math.max(0,Math.min(100,Math.round(x)))}
function score(cur,s){let z=health(s);if(genre(cur)===genre(s))z+=28;const a=tags(cur),b=tags(s);let n=0;a.forEach(x=>{if(b.has(x))n++});z+=Math.min(30,n*10);if((s?.bitrate||0)>=128)z+=5;return z}
function recent(){try{return JSON.parse(sessionStorage.getItem('trP1ZapRecent')||'[]')}catch(e){return[]}}
function saveRecent(a){try{sessionStorage.setItem('trP1ZapRecent',JSON.stringify(a.slice(0,8)))}catch(e){}}
function msg(t){try{toast(t)}catch(e){}}
function zap(){try{if(!Array.isArray(stations)||stations.length<2||typeof index!=='number'||index<0){msg('Zapping için radyo listesi hazırlanıyor');return}const cur=stations[index],used=recent();let a=stations.map((s,i)=>({s,i,k:k(s),score:score(cur,s)})).filter(x=>x.i!==index&&x.k&&!used.includes(x.k));if(!a.length)a=stations.map((s,i)=>({s,i,k:k(s),score:score(cur,s)})).filter(x=>x.i!==index&&x.k);a.sort((x,y)=>y.score-x.score);const top=a.slice(0,Math.min(5,a.length));if(!top.length){msg('Uygun radyo bulunamadı');return}const pick=top[Math.floor(Math.random()*top.length)];saveRecent([pick.k,...used.filter(x=>x!==pick.k)]);select(pick.i,true);msg('Zapping • '+pick.s.name+' • '+health(pick.s)+'/100')}catch(e){msg('Zapping başlatılamadı')}}
function intercept(e){const b=e.target?.closest?.(SEL);if(!b||b.dataset.p1Zap!=='1'||!isP1())return;e.preventDefault();e.stopImmediatePropagation();zap()}
function boot(){window.addEventListener('click',intercept,true);[0,160,480,1000].forEach(ms=>setTimeout(patch,ms));document.addEventListener('click',e=>{if(e.target.closest('[data-prof],.nature-profile-pill')){setTimeout(patch,40);setTimeout(patch,260);setTimeout(patch,800)}},true);window.addEventListener('turkradyo-theme-synced',()=>setTimeout(patch,0));window.addEventListener('pageshow',()=>setTimeout(patch,0));document.addEventListener('visibilitychange',()=>{if(!document.hidden)setTimeout(patch,0)})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();