(function(){'use strict';
let applied=false;
function fixMarkedLeftBlock(){try{if(!window.scene||applied)return;scene.traverse(o=>{if(!o.isInstancedMesh||!o.geometry)return;const p=o.geometry.parameters||{};const isSeat=o.geometry.type==='BoxGeometry'&&Math.abs((p.width||0)-.46)<.03&&Math.abs((p.depth||0)-.43)<.04;const isBack=o.geometry.type==='BoxGeometry'&&Math.abs((p.width||0)-.46)<.03&&Math.abs((p.height||0)-.48)<.04;if(!isSeat&&!isBack)return;const mat=new THREE.Matrix4(),pos=new THREE.Vector3(),q=new THREE.Quaternion(),sc=new THREE.Vector3();for(let i=0;i<o.count;i++){o.getMatrixAt(i,mat);mat.decompose(pos,q,sc);if(pos.x<-3&&pos.z>0){q.setFromEuler(new THREE.Euler(0,-Math.PI/2,0));mat.compose(pos,q,sc);o.setMatrixAt(i,mat)}}o.instanceMatrix.needsUpdate=true});applied=true}catch(e){}}
function tick(){fixMarkedLeftBlock();requestAnimationFrame(tick)}requestAnimationFrame(tick);
})();