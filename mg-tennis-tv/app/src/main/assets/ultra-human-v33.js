(function(){
'use strict';
let ultra=null,mixer=null,handBone=null,racket=null,lastHit='',contactUntil=0,prevBallVz=0,loading=false,ready=false;
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
function bone(root,pats){let f=null;root.traverse(o=>{if(f||!o.isBone)return;const n=(o.name||'').toLowerCase();if(pats.some(p=>n.includes(p)))f=o});return f}
function hideOld(){try{scene.traverse(o=>{if(!o||!o.name||o===ultra)return;if(/GUARANTEED_HUMAN_OPPONENT|ULTRA_REALISTIC_TENNIS_OPPONENT|REAL_TENNIS_OPPONENT/.test(o.name))o.visible=false})}catch(e){}}
function tune(root){root.traverse(o=>{if(!(o.isMesh||o.isSkinnedMesh))return;o.visible=true;o.castShadow=true;o.receiveShadow=true;o.frustumCulled=false;if(Array.isArray(o.material))o.material.forEach(tuneMat);else if(o.material)tuneMat(o.material)})}
function tuneMat(m){m.side=THREE.DoubleSide;m.transparent=false;m.opacity=1;m.depthTest=true;m.depthWrite=true;m.roughness=clamp(m.roughness==null?.55:m.roughness,.3,.82);m.metalness=clamp(m.metalness||0,0,.08);m.needsUpdate=true}
function loadPart(url){return new Promise((res,rej)=>loader.load(url,g=>res(g),undefined,rej))}
function fit(root){root.updateMatrixWorld(true);let b=new THREE.Box3().setFromObject(root),h=b.max.y-b.min.y;if(!isFinite(h)||h<.2)h=1.8;root.scale.multiplyScalar(1.84/h);root.updateMatrixWorld(true);b=new THREE.Box3().setFromObject(root);root.position.y-=b.min.y}
async function build(){if(ready||loading||!window.THREE||!window.scene||!window.loader)return;loading=true;try{const parts=await Promise.all([loadPart('v33_body.glb'),loadPart('v33_head.glb'),loadPart('v33_hair.glb')]);const g=new THREE.Group();g.name='MG_ULTRA_PHOTOREAL_OPPONENT_V35';const body=parts[0].scene,head=parts[1].scene,hair=parts[2].scene;tune(body);tune(head);tune(hair);g.add(body,head,hair);fit(g);g.position.set(0,0,-8.15);g.rotation.y=0;g.visible=true;scene.add(g);ultra=g;window.opponent=g;window.MGGuaranteedOpponent=g;handBone=bone(body,['righthand','right_hand','hand_r','mixamorigrighthand']);if(typeof createRacket==='function'){racket=createRacket(0x151515);racket.name='MG_REAL_RACKET_V35';racket.scale.setScalar(.72);scene.add(racket);window.opponentRacket=racket}mixer=new THREE.AnimationMixer(body);const clips=parts[0].animations||[];const idle=clips.find(a=>/idle|sway/i.test(a.name))||clips[0];if(idle)mixer.clipAction(idle).play();ready=true;loading=false;hideOld();const st=document.getElementById('status');if(st)st.textContent='RAKİP HAZIR';}catch(e){console.error('human load failed',e);loading=false;setTimeout(build,1500)}}
function racketPose(){if(!ultra||!racket)return null;const p=new THREE.Vector3(),q=new THREE.Quaternion();try{if(handBone){handBone.getWorldPosition(p);handBone.getWorldQuaternion(q);racket.position.copy(p);racket.quaternion.copy(q);racket.rotateZ(-.55);racket.translateY(-.32);racket.translateX(.12)}else{ultra.getWorldPosition(p);racket.position.set(p.x+.52,1.03,p.z+.08);racket.rotation.set(0,.15,-.55)}return p}catch(e){return null}}
function racketContactPoint(){if(!racket)return null;const p=new THREE.Vector3();try{new THREE.Box3().setFromObject(racket).getCenter(p);p.y=clamp(p.y,.55,1.8);return p}catch(e){return null}}
function lockBallToRacket(){try{if(!ball||!ballVel||!ultra)return;const now=performance.now(),cur=String(window.lastHitter||'');const switched=cur==='opponent'&&lastHit!=='opponent',reversed=prevBallVz<-.15&&ballVel.z>.15;if((switched||reversed)&&now>contactUntil){const cp=racketContactPoint();if(cp){ball.position.copy(cp);if(ballShadow)ballShadow.position.set(cp.x,.02,cp.z);contactUntil=now+180}}if(now<contactUntil){const cp=racketContactPoint();if(cp)ball.position.lerp(cp,.92)}lastHit=cur;prevBallVz=ballVel.z}catch(e){}}
function moveOpponent(dt){try{if(!ultra)return;ultra.visible=true;let tx=0,tz=-8.15;if(ball&&ballVel&&ballVel.z<-.2){const eta=(-8-ball.position.z)/ballVel.z;if(eta>0&&eta<4)tx=clamp(ball.position.x+ballVel.x*eta,-4.2,4.2)}const k=1-Math.exp(-4.5*dt);ultra.position.x+=(tx-ultra.position.x)*k;ultra.position.z+=(tz-ultra.position.z)*k;if(typeof opponentX!=='undefined')opponentX=ultra.position.x;if(typeof opponentZ!=='undefined')opponentZ=ultra.position.z}catch(e){}}
let t0=performance.now();function tick(t){const dt=Math.min(.04,(t-t0)/1000||.016);t0=t;if(!ready)build();if(ultra){ultra.visible=true;if(mixer)mixer.update(dt);moveOpponent(dt);racketPose();lockBallToRacket();hideOld()}requestAnimationFrame(tick)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>requestAnimationFrame(tick));else requestAnimationFrame(tick);
})();