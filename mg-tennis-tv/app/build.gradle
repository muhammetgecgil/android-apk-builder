plugins {
    id 'com.android.application'
}
android {
    namespace 'com.mg.tennistv'
    compileSdk 35
    defaultConfig { applicationId 'com.mg.tennistv'; minSdk 26; targetSdk 35; versionCode 56; versionName '5.6.0' }
    signingConfigs { release { storeFile file('../signing/mg-tennis-update.jks'); storePassword 'MGtennis2026!'; keyAlias 'mgtennis'; keyPassword 'MGtennis2026!' } }
    buildTypes { release { signingConfig signingConfigs.release; minifyEnabled false; shrinkResources false } }
}
dependencies {
    implementation "androidx.webkit:webkit:1.17.0"
    implementation "androidx.core:core:1.15.0"
    implementation "com.google.mlkit:pose-detection:18.0.0-beta5"
}
