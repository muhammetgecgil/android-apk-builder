name: Build 3D Model Viewer CAD Mobile UI

on:
  push:
    branches: [ camera3d-cad-v140-release ]
    paths:
      - 'modelviewer/**'
      - 'settings.gradle'
      - '.github/mg3d-release.jks.b64'
      - '.github/workflows/build-modelviewer.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
      - name: Install Android API 36
        run: sdkmanager "platforms;android-36" "build-tools;35.0.0"
      - name: Restore persistent MG 3D signing key
        run: base64 -d .github/mg3d-release.jks.b64 > mg3d-release.jks
      - name: Apply source upgrades
        run: |
          python3 modelviewer/upgrade_units_playstore.py
          python3 modelviewer/upgrade_cad_v140.py
          python3 modelviewer/upgrade_cad_v160.py
          python3 modelviewer/upgrade_cad_v170.py
          python3 modelviewer/upgrade_cad_v171.py
          sed -i "s/versionCode 15/versionCode 20/" modelviewer/build.gradle
          sed -i "s/versionName '1.3.2'/versionName '1.7.1'/" modelviewer/build.gradle
      - name: Bundle pinned offline CAD engine
        run: |
          set -euxo pipefail
          AS=modelviewer/src/main/assets/cadviewer
          mkdir -p "$AS" /tmp/occt /tmp/three
          cd /tmp/occt
          npm pack occt-import-js@0.0.23
          tar -xzf occt-import-js-0.0.23.tgz
          cp package/dist/occt-import-js.js "$GITHUB_WORKSPACE/$AS/occt-import-js.js"
          cp package/dist/occt-import-js.wasm "$GITHUB_WORKSPACE/$AS/occt-import-js.wasm"
          cd /tmp/three
          npm pack three@0.124.0
          tar -xzf three-0.124.0.tgz
          cp package/build/three.min.js "$GITHUB_WORKSPACE/$AS/three.min.js"
          cp package/examples/js/controls/OrbitControls.js "$GITHUB_WORKSPACE/$AS/OrbitControls.js"
          test -s "$GITHUB_WORKSPACE/$AS/occt-import-js.wasm"
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Build signed APK and AAB
        run: gradle --no-daemon :modelviewer:assembleRelease :modelviewer:bundleRelease --stacktrace
      - name: Verify Mobile UI package and fixed certificate
        run: |
          APK=modelviewer/build/outputs/apk/release/modelviewer-release.apk
          AAB=modelviewer/build/outputs/bundle/release/modelviewer-release.aab
          test -s "$APK"
          test -s "$AAB"
          $ANDROID_HOME/build-tools/35.0.0/apksigner verify --verbose --print-certs "$APK" | tee cert.txt
          $ANDROID_HOME/build-tools/35.0.0/aapt dump badging "$APK" | tee badging.txt
          grep -q "package: name='com.muhammetgecgil.mg3dmodelviewer'" badging.txt
          grep -q "versionCode='20'" badging.txt
          grep -q "versionName='1.7.1'" badging.txt
          grep -q "application-icon" badging.txt
          grep -qi "cff09eb09175519edd966d02d00a36027d047b59ba8ca03923a3a44c39615dc7" cert.txt
          unzip -l "$APK" | grep -q 'assets/cadviewer/occt-import-js.wasm'
          unzip -l "$APK" | grep -q 'assets/cadviewer/cad-v170.js'
          unzip -l "$APK" | grep -q 'assets/cadviewer/cad-v171.js'
          unzip -p "$APK" assets/cadviewer/cad-v171.js | grep -q 'safe-area-inset-right'
          unzip -p "$APK" assets/cadviewer/cad-v171.js | grep -q 'mgToolsCollapsed'
          unzip -p "$APK" assets/cadviewer/cad-v171.js | grep -q 'grid-template-columns'
          cp "$APK" MG_3D_Model_Viewer_v1.7.1_MOBILE_UI_FIX.apk
          cp "$AAB" MG_3D_Model_Viewer_v1.7.1_MOBILE_UI_FIX.aab
          sha256sum MG_3D_Model_Viewer_v1.7.1_MOBILE_UI_FIX.apk MG_3D_Model_Viewer_v1.7.1_MOBILE_UI_FIX.aab > SHA256SUMS.txt
      - name: Upload verified Mobile UI Fix
        uses: actions/upload-artifact@v4
        with:
          name: MG-3D-Model-Viewer-v1.7.1-MOBILE-UI-FIX
          path: |
            MG_3D_Model_Viewer_v1.7.1_MOBILE_UI_FIX.apk
            MG_3D_Model_Viewer_v1.7.1_MOBILE_UI_FIX.aab
            SHA256SUMS.txt
          if-no-files-found: error
          retention-days: 30
