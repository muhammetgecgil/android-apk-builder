name: Build Whiffletree Aero APK

permissions:
  contents: write

on:
  push:
    branches: [ "main", "feature/whiffletree-aero-v1" ]
    paths:
      - 'whiffletree-aero/**'
      - '.github/workflows/ci-whiffletree-aero.yml'
  pull_request:
    paths:
      - 'whiffletree-aero/**'
      - '.github/workflows/ci-whiffletree-aero.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Compile-check engineering source
        working-directory: whiffletree-aero
        run: gradle --no-daemon clean compileDebugJavaWithJavac --stacktrace
      - name: Restore stable Whiffletree signing key when available
        uses: actions/cache@v4
        with:
          path: ~/.android/debug.keystore
          key: whiffletree-aero-debug-signing-v1
          fail-on-cache-miss: false
      - name: Build installable test APK
        working-directory: whiffletree-aero
        run: gradle --no-daemon assembleDebug --stacktrace
      - name: Rename APK
        run: cp whiffletree-aero/app/build/outputs/apk/debug/app-debug.apk Whiffletree-Aero-v7.19.4.apk
      - name: Upload installable APK
        uses: actions/upload-artifact@v4
        with:
          name: whiffletree-aero-v7.19.4
          if-no-files-found: error
          path: Whiffletree-Aero-v7.19.4.apk
