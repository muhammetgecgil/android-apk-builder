name: Build Hafiza Dostum v4 APK

on:
  push:
    branches: [ main ]
    paths:
      - 'hafiza-dostum/**'
      - '.github/workflows/build-hafiza-dostum.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
      - name: Install Android platform
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Build debug APK
        run: gradle --no-daemon -p hafiza-dostum :app:assembleDebug --stacktrace
      - name: Rename APK
        run: cp hafiza-dostum/app/build/outputs/apk/debug/app-debug.apk HafizaDostum-v4.0.apk
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: HafizaDostum-v4.0
          path: HafizaDostum-v4.0.apk
          if-no-files-found: error
          retention-days: 30
